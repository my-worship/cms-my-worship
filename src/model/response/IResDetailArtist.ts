export interface IResDetailArtist {
  name: string;
  slug: string;
  description?: string;
  created_by?: string;
  image: string;
  request_note: string;
  is_revision: boolean;
  revision_notes: string;
  created_date: string;
  publish_date: string;
}
